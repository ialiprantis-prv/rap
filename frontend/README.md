# @rap/frontend — stub

Seeded at the frontend rung (later commit). At C0 this workspace is a placeholder
so `npm install` resolves the three-workspace graph. The UI will reuse
`@rap/engine` for every methodology computation — the browser never reimplements
the formula.

Not yet wired into the root `tsc -b` project references (nothing to build here
until the rung lands).
